#include "main.h"
/**
 * _abs - This function that checks for lowercase character.
 * @r: checks for return
 * Return: on Sucess 1
 * On error, 0 is returned
 */

int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}